# Configuración de pruebas para PedidoService
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Configuración de logging para pruebas
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.test=INFO
logging.level.ec.edu.espe=DEBUG

# TestContainers configurará dinámicamente:
# - spring.datasource.url
# - spring.datasource.username
# - spring.datasource.password

# Desactivar seguridad adicional para algunas pruebas
# (Spring Security Test ya maneja esto con @WithMockUser)
